<launch>

    <!-- 设置参数 -->
    <param name="robot_description" textfile="$(find urdf_learn)/urdf/base_learn.urdf" />

    <!-- 启动 rviz -->
    <node pkg="rviz" type="rviz" name="rviz" args="-d $(find urdf_learn)/config/base_learn.rviz"/>

</launch>
